const ProductsPreview = (title, products) => {
    return (
        <div>
            <h2>{title}</h2>

            <div>
                {products.map((product) => (
                    
                ))}
            </div>
        </div>
    )
}

export default ProductsPreview